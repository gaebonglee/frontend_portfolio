import ProjectSlide from "components/slide/ProjectSlide";

export default function SlideSection() {
    return(<section className="section--slide-Home">
        <ProjectSlide/>
    </section>)
}